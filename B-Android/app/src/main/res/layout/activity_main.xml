<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical">
    <LinearLayout android:layout_width="match_parent" android:layout_height="56dp"
        android:gravity="center_vertical" android:paddingHorizontal="8dp">
        <ImageButton android:id="@+id/back" android:layout_width="48dp" android:layout_height="48dp"
            android:contentDescription="Kembali" android:src="@android:drawable/ic_media_previous" />
        <EditText android:id="@+id/address" android:layout_width="0dp" android:layout_height="44dp"
            android:layout_weight="1" android:singleLine="true" android:hint="Cari atau masukkan alamat"
            android:inputType="textUri" android:paddingHorizontal="12dp" />
        <ImageButton android:id="@+id/reload" android:layout_width="48dp" android:layout_height="48dp"
            android:contentDescription="Muat ulang" android:src="@android:drawable/ic_popup_sync" />
        <ImageButton android:id="@+id/menu" android:layout_width="48dp" android:layout_height="48dp"
            android:contentDescription="Menu" android:src="@android:drawable/ic_menu_more" />
    </LinearLayout>
    <ProgressBar android:id="@+id/progress" style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent" android:layout_height="3dp" android:max="100" />
    <WebView android:id="@+id/webView" android:layout_width="match_parent"
        android:layout_height="0dp" android:layout_weight="1" />
</LinearLayout>
